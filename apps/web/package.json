{
  "name": "scenecut-web-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-wasm": "mkdir -p public/wasm && cp -f ../../packages/core/dist/detection.wasm.js public/wasm/detection.wasm.js 2>/dev/null || echo 'NOTE: WASM build artifacts not found. Run bun run build:core:wasm at the repo root.' && cp -f ../../packages/core/dist/detection.wasm public/wasm/detection.wasm 2>/dev/null || true",
    "dev": "bun run copy-wasm && vite",
    "build": "bun run copy-wasm && tsc && vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "@doedja/scenecut-core": "^0.1.6",
    "@doedja/scenecut-web": "^0.1.6"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.4.10"
  }
}
