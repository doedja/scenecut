{
  "name": "scenecut-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "scenecut monorepo — core (WASM + detector), node (@doedja/scenecut), web (@doedja/scenecut-web), apps/web (browser app)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:core": "bun --filter='./packages/core' run build",
    "build:core:ts": "bun --filter='./packages/core' run build:ts",
    "build:core:wasm": "bun --filter='./packages/core' run build:wasm",
    "build:node": "bun --filter='./packages/node' run build",
    "build:web": "bun --filter='./packages/web' run build",
    "build:app": "bun --filter='./apps/web' run build",
    "build": "bun run build:core && bun run build:node && bun run build:web && bun run build:app",
    "build:site": "bun run build:core:ts && bun run build:web && bun run build:app",
    "dev:app": "bun --filter='./apps/web' run dev",
    "clean": "rm -rf packages/*/dist apps/*/dist"
  },
  "license": "GPL-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/doedja/scenecut.git"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
